require 'pry'
class PigLatinizer

  def sentence_filter(user_phrase)
  array = user_phrase.split(" ")
  end

  def latinize(array)
    array.map do |word|
    if word[0]
    else
    end
  end.join('')
  end



end
